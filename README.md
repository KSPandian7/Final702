## Open the project folder in VS Code
   **(Folder must contain: app.py, requirements.txt, sketch_photo_triplet_model.pth, data/)**

## Open VS Code Terminal
   ```
   Shortcut: Ctrl + `
   ```

## (Optional but recommended) Activate virtual environment

```
   Windows:
   .venv\Scripts\activate

   Linux / Mac:
   source .venv/bin/activate
```

## Install dependencies (only once)
```
   pip install -r requirements.txt
```

## Run the Streamlit app
```
   streamlit run app.py
```
## Open browser and go to
```
   http://localhost:8501
```

## Upload a sketch image (.jpg / .png)

## View Top-K retrieved photos

**DONE.**
